import socket
import os

def controller(receiving_queue,sending_queue):


    # Treat infos from Images_recognition
    # Treat infos from other cars
    # treat infos from the computer to mosve the car (If leader car only)
